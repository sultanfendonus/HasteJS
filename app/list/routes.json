{
  "routes": [
    {
      "method": "GET",
      "path": "/list",
      "controller": "list.find",
      "config": {
        "middleware": ["isAuthenticate2"]
      }
    },
    {
      "method": "GET",
      "path": "/list/count",
      "controller": "list.count",
      "config": {
        "middleware": ["isAuthenticate", "isAuthenticate2"]
      }
    },
    {
      "method": "GET",
      "path": "/list/:id",
      "controller": "list.findOne",
      "config": {
        "middleware": ["isAuthenticate"]
      }
    },
    {
      "method": "POST",
      "path": "/list",
      "controller": "list.create",
      "config": {
        "middleware": ["isAuthenticate"]
      }
    },
    {
      "method": "PUT",
      "path": "/list/:id",
      "controller": "list.update",
      "config": {
        "middleware": ["isAuthenticate"]
      }
    },
    {
      "method": "DELETE",
      "path": "/list/:id",
      "controller": "list.delete",
      "config": {
        "middleware": ["isAuthenticate"]
      }
    }
  ]
}