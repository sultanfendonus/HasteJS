import isAuthenticate from "./isAuthenticate.js";
import isAuthenticate2 from "./isAuthenticate2.js";

export const MIDDLEWARE_MAPPER = {
    isAuthenticate: isAuthenticate,
    isAuthenticate2: isAuthenticate2,
}